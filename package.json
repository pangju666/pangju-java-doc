{
    "scripts": {
        "docs:dev": "vitepress dev docs",
        "docs:build": "node scripts/replace-placeholders.js && vitepress build docs",
        "docs:preview": "vitepress preview docs"
    },
    "devDependencies": {
        "dotenv": "^17.2.3",
        "vitepress": "^1.6.4"
    }
}
